# Textalyzer
Sensible metrics - textalyzer toolkit

Install dependencie:

```sh
mvn install
```

Build package with dependencies:

```sh
mvn clean compile assembly:single
```

Run program cycle:

```sh
mvn package
```